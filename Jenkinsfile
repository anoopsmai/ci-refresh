pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh '''
                echo "from JenkinsFile in Repo" 
                  '''
            }
        }
    }
}

